services:
  - type: web
    name: agente-proveedores
    runtime: bun
    plan: free
    buildCommand: bun install
    startCommand: bun run src/server.ts
    envVars:
      - key: GEMINI_API_KEY
        sync: false